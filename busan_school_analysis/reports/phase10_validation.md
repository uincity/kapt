# Phase 10 Elementary Demand 증분 설명력 검증

## 불변 입력과 Phase 8 재현
- Phase 7·8·9 입력 해시는 분석 전후 동일하다.
- Phase 8 재현값: Pearson `0.398`, Spearman `0.375`, 구·군 내 `0.190`, 법정동 demeaned `0.005`.
- 법정동 FE M 10점 연관은 `3.10%`, p-value `0.1887`다.

## Phase 9.5
- E와 학교 단위 M의 Pearson은 `0.354`, Spearman은 `0.283`다.
- 두 점수 모두 상위 25%인 학교는 `31`개다.
- 해당 학교: 센텀초등학교, 여고초등학교, 해강초등학교, 분포초등학교, 해원초등학교, 해운대초등학교, 용문초등학교, 부흥초등학교, 해림초등학교, 사직초등학교, 남문초등학교, 상당초등학교, 용소초등학교, 강동초등학교, 창신초등학교, 거제초등학교, 신곡초등학교, 두실초등학교, 명륜초등학교, 예원초등학교, 신도초등학교, 장서초등학교, 금강초등학교, 해동초등학교, 금정초등학교, 교동초등학교, 남천초등학교, 구서초등학교, 용수초등학교, 장산초등학교, 부곡초등학교.
- Common sample은 Phase 8 Primary `487`개 중 `486`개 단지, 최근 12개월 거래 `22,181`건이다. E 불완전 때문에 `1`개 단지와 `37`건이 제외됐다.

## 증분 검증
- Common sample M의 법정동 내 Spearman은 `0.004`, E+M school component는 `0.302`로 변화량은 `+0.298`다. 원 Phase 8 benchmark 0.005와 common-sample 결과를 구분해야 한다.
- M을 통제한 E의 법정동 FE 계수는 `0.00584`이며 E 10점당 `6.01%`, p-value `0.0000`다.
- E×M 계수는 `-0.01631`, p-value `0.9864`다. Interaction 판정은 **NOT_SUPPORTED**이다.
- 고정된 3개 단지 Group Split 평균에서 M-only log RMSE는 `0.29059`, E+M은 `0.27872`로 `+4.08%` 개선이며 세 split 모두 같은 방향이었다. Interaction 추가는 E+M 대비 `-2.33%` 변화다.
- Time-aligned pooled FE의 E 10점 연관은 `-0.14%`, p-value `0.9739`로 구조적 횡단면 결과를 통계적으로 재현하지 못했다.
- Elementary Demand Layer 최종 판정은 **INCREMENTAL_VALUE**이다. Phase 11 검토 조건 충족 여부는 **YES**다.

## 질문별 결론
1. E와 M의 Spearman은 `0.283`로 중간 이하의 양의 중복성을 보이며 서로 완전히 같은 정보를 측정하지 않는다.
2. E 단독 법정동 내 Spearman `0.307`와 M 통제 후 FE 결과는 E에 별도 정보가 있음을 지지한다.
3. 두 점수 모두 상위 25%인 학교는 `31`개다.
4. 전체 학교명과 부산·구군 순위는 `phase95_high_e_high_m_schools.csv`에 기록했다.
5. HIGH_E_HIGH_M 연결 아파트 `86`개는 D 대비 물리·법정동 통제 residual 기준 `8.85%` 높았다. 이는 조건부 연관이며 인과 프리미엄 추정치가 아니다.
6. M 통제 후 E 10점 연관은 `6.01%`(p=`5.408e-09`)다. 반대로 E 통제 후 M 10점 연관은 `1.15%`(p=`0.6078`)다.
7. 선형 E×M은 p=`0.9864`이고 ML interaction 추가도 E+M보다 RMSE가 악화되어 **NOT_SUPPORTED**다. 두 조건의 단순 합을 넘는 비선형 premium은 확인되지 않았다.
8. 원 Phase 8 법정동 demeaned Spearman `0.005`는 common M에서 `0.004`, E+M에서 `0.302`였다.
9. Out-of-sample E+M log RMSE는 M-only보다 `4.08%` 개선됐고, R²는 `0.669`에서 `0.695`로 상승했다.
10. Time-aligned E의 법정동 내 Spearman은 2024 `0.052`, 2025 `0.211`, 2026 `0.300`였지만 pooled FE는 유의하지 않았다. 따라서 방향성은 일부 유지됐으나 회귀 강건성은 유지되지 않았다.

## 보조 검증과 사례
- 물리·위치 controls-only residual과 E의 Spearman은 `0.182`, 법정동 내부 Spearman은 `0.251`다.
- 연식 ±5년·유사 세대수 조건을 만족한 동일 법정동 사례는 `30`개 동이며, 높은 EM 단지가 더 비싼 사례 비율은 `60.0%`다. 개별 사례는 다른 미관측 품질 차이를 포함할 수 있다.
- 센텀초는 E `89.53`, 학교 단위 M `99.13`, 두 점수 모두 부산 100 percentile이며 연결 500세대 이상 단지는 `3`개다. 최근 ㎡당 중앙가격은 `11,931,088`원이다.
- API 미관측 학교 영향은 `phase10_missing_school_impact.csv`에 기록했다. Primary에서 제외된 사례는 봉삼초 연결 1개 단지·37건이다.

## 시간 및 해석 한계
- Structural 분석은 2026 E를 현재 학군특성 proxy로 사용하며 인과·과거시점 예측으로 해석하지 않는다.
- Time-aligned 분석은 2024~2026 각 거래연도와 같은 공시연도의 E만 연결했다. 2024 점수는 당해 횡단면 변수만 사용하며 미래 성장정보를 사용하지 않는다.
- SHAP 패키지가 없어 SHAP 값으로 가장하지 않고 동일 Tree model의 2D PDP를 사용했다.
- 센텀초는 결과 확인용 사례이며 score나 threshold 조정에 사용하지 않았다. override 미적용 Phase 7 점수는 불변 artifact로 남아 있지 않아 재계산하지 않았다.
- Phase 11 검토 조건은 충족했지만 이번 단계에서 임의 통합점수는 생성하지 않았다.
